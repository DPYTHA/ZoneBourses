[build]
builder = "nixpacks"
nixpacksPlan = """
  providers.python = "3.11";
  install.aptPackages = ["libpq-dev", "gcc", "python3-dev"];
  install.pipInstall = ["-r", "requirements.txt"];
"""

[deploy]
startCommand = "gunicorn app:app"